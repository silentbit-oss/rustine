#include "openaptx.h"
#include "test_common.h"

// void test_sync_insert() {
//     struct aptx_context *ctx = aptx_init(0);

//     uint8_t hd_before = aptx_get_hd(ctx);
//     aptx_insert_sync(ctx);  
//     ASSERT(aptx_get_hd(ctx) != hd_before, "HD should change after sync");
//     PASS("aptX sync caused HD change");

//     aptx_finish(ctx);
// }
