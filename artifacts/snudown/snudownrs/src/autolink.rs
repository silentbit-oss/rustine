use crate::*;

pub fn sd_autolink_issafe(/* const uint8_t *link, size_t link_len */) {
	todo!();
}
pub fn autolink_delim(/* uint8_t *data, size_t link_end, size_t max_rewind, size_t size */) {
	todo!();
}
pub fn check_reddit_autolink_prefix(/* const uint8_t *data, size_t max_rewind, size_t max_lookbehind, size_t size, char prefix_char */) {
	todo!();
}
pub fn check_domain(/* uint8_t *data, size_t size, int allow_short */) {
	todo!();
}
pub fn sd_autolink__email(/* size_t *rewind_p, struct buf *link, uint8_t *data, size_t max_rewind, size_t size, unsigned int flags */) {
	todo!();
}
pub fn sd_autolink__subreddit(/* size_t *rewind_p, struct buf *link, uint8_t *data, size_t max_rewind, size_t max_lookbehind, size_t size, int *no_slash */) {
	todo!();
}
pub fn sd_autolink__username(/* size_t *rewind_p, struct buf *link, uint8_t *data, size_t max_rewind, size_t max_lookbehind, size_t size, int *no_slash */) {
	todo!();
}
pub fn sd_autolink__www(/* size_t *rewind_p, struct buf *link, uint8_t *data, size_t max_rewind, size_t size, unsigned int flags */) {
	todo!();
}
pub fn sd_autolink__url(/* size_t *rewind_p, struct buf *link, uint8_t *data, size_t max_rewind, size_t size, unsigned int flags */) {
	todo!();
}
