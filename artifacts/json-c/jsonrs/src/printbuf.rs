use crate::*;

// struct printbuf
// {
//   char *buf;
//   int bpos;
//   int size;
// }

pub fn printbuf_free(/* struct printbuf *p */) {
	todo!();
}
pub fn printbuf_new(/* void */) {
	todo!();
}
pub fn printbuf_reset(/* struct printbuf *p */) {
	todo!();
}
pub fn printbuf_extend(/* struct printbuf *p, int min_size */) {
	todo!();
}
pub fn printbuf_memappend(/* struct printbuf *p, const char *buf, int size */) {
	todo!();
}
pub fn printbuf_memset(/* struct printbuf *pb, int offset, int charvalue, int len */) {
	todo!();
}
pub fn sprintbuf(/* struct printbuf *p, const char *msg, ... */) {
	todo!();
}
